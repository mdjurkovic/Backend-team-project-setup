const bookMutations = {
  createBook: async (_, { book }, { loaders }) => {
    return { }
  },
  updateBook: async (_, { id, book }, { loaders }) => {
    return { }
  },
};

export default bookMutations;
